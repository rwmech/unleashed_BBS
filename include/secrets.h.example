/*
 * File:        secrets.h.example
 * Description: Copy to include/secrets.h (git-ignored) and fill in.
 *              Wi-Fi only; everything else lives in data/system.cfg.
 * Listing:     COMPLETE FILE
 */
#pragma once
#define WIFI_SSID "your-ssid"      // case-sensitive
#define WIFI_PASS "your-password"
